apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: pr-vcluster-internal-argocd
  namespace: argocd
spec:
  generators:
    - pullRequest:
        github:
          appSecretName: loft-demo-org-cred
          labels:
            - create-prvcluster
          owner: loft-demos
          repo: {REPLACE_REPO_NAME}
        requeueAfterSeconds: 30
        template:
          metadata: {}
          spec:
            destination: {}
            project: ''
  template:
    metadata:
      name: {REPLACE_REPO_NAME}-pr-{{number}}
    spec:
      destination:
        name: in-cluster
        namespace: p-api-framework
      info:
        - name: PRvCluster Argo CD URL
          value: https://argocd-{REPLACE_REPO_NAME}-pr-{{number}}.us.demo.dev
        - name: GitHub PR
          value: https://github.com/loft-demos/{REPLACE_REPO_NAME}/pull/{{number}}
      project: default
      source:
        kustomize:
          namePrefix: {REPLACE_REPO_NAME}-pr-{{number}}-
          patches:
            - patch: |-
                - op: replace
                  path: /spec/prNumber
                  value: '{{number}}'
              target:
                kind: PullRequestEnvironment
                name: vcluster
        path: kustomize-pr/
        repoURL: https://github.com/loft-demos/{REPLACE_REPO_NAME}.git
        targetRevision: '{{head_sha}}'
      syncPolicy:
        automated:
          selfHeal: true
